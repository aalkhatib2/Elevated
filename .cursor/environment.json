{
  "name": "Elevated landing page",
  "install": "test -f index.html && test -f hero-elevated.jpg && test -f elevated-logo.jpg && python3 -c \"import sys; print('Static landing page files present; Python', sys.version.split()[0])\"",
  "start": "bash .cursor/start-landing.sh",
  "terminals": [
    {
      "name": "Landing page logs",
      "command": "touch /tmp/elevated-landing.log; tail -f /tmp/elevated-landing.log",
      "description": "HTTP access logs for the static Elevated landing page on port 5173"
    }
  ],
  "ports": [
    {
      "name": "Landing page",
      "port": 5173
    }
  ]
}
